import { createMuiTheme } from '@material-ui/core/styles';
import './theme.css';

export const mainTheme = createMuiTheme({
    palette: {
        primary: {
            main: "rgb(162,60,24)",
        }
    }
});
