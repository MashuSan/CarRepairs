import { createMuiTheme } from '@material-ui/core/styles';
import './theme.css';

export const mainTheme = createMuiTheme({
  palette: {
    primary: {
        main: "#a61b1b",
    },
    secondary: {
        main: '#f44336',
    },
  },
});
